@model string[]
@{
    ViewBag.Title = "List";
    var newList = ViewBag.data as string[];
    var newList1 = ViewData["data"] as string[];
}

<h2>List</h2>

<ol>
    @foreach (var item in newList)
    {
        <li>@item</li>
    }
</ol>
<ol>
    @foreach (var item in Model)
    {
        <li>@item</li>
    }
</ol>
<ol>
    @for (int i = 0; i < newList1.Length; i++)
    {
        <li>@newList1[i]</li>
    }
</ol>
<ol>
    @for (int i = 0; i < Model.Length; i++)
    {
        <li>@Model[i]</li>
    }
</ol>